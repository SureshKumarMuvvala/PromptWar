[project]
name = "emergency-health-assistant"
version = "1.0.0"
description = "Production-ready Emergency Health Assistant API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
